@inherits BaseButtonComponent

<ButtonComponent 
    Caption="@(Caption ?? "Edit")"
    Color="@(Color ?? "secondary")"
    Icon="@(Icon ?? "")"
    OnClick="@OnClick"
    TooltipStr="@(TooltipStr ?? "Edit")"
    IsOutline="@(IsOutline)"
    />