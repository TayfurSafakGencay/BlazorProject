@inherits BaseButtonComponent

<ButtonComponent 
    Caption="@(Caption ?? "Edit")"
    Color="@(Color ?? "primary")"
    Icon="@(Icon ?? "")"
    OnClick="@OnClick"
    TooltipStr="@(TooltipStr ?? "Edit")"
    IsOutline="@(IsOutline)"
    />